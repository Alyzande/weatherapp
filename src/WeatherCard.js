import React from "react";

function WeatherCard() {
  return (
    <div className="WeatherCard">
      weathersDate, image, degreeDisplay, weatherText, windInfo
    </div>
  );
}

export default WeatherCard;
